export type AdminSessionContext = {
  accessToken: string;
  adminUserId: string;
  adminUserName: string;
  email: string;
  hotelId: string;
  role: string;
  permissions: string[];
  sessionId: string;
};
