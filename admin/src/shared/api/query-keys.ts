export const adminQueryKeys = {
  root: (scope: string) => ['admin', scope] as const,
  dashboard: (scope: string) => [...adminQueryKeys.root(scope), 'dashboard'] as const,
  guests: (scope: string) => [...adminQueryKeys.root(scope), 'guests'] as const,
  guestList: (scope: string, search: string) => [...adminQueryKeys.guests(scope), { search }] as const,
  services: (scope: string) => [...adminQueryKeys.root(scope), 'services'] as const,
  requests: (scope: string) => [...adminQueryKeys.root(scope), 'requests'] as const,
  requestList: (scope: string, filters: { search: string; status: string }) => [...adminQueryKeys.requests(scope), filters] as const,
  concierge: (scope: string) => [...adminQueryKeys.root(scope), 'concierge'] as const,
  conversations: (scope: string, search: string) => [...adminQueryKeys.concierge(scope), 'conversations', { search }] as const,
  messages: (scope: string, stayId: string) => [...adminQueryKeys.concierge(scope), 'messages', stayId] as const,
  stays: (scope: string) => [...adminQueryKeys.root(scope), 'stays'] as const,
  stayList: (scope: string, filters: object) => [...adminQueryKeys.stays(scope), filters] as const,
  stayConsumption: (scope: string, stayId: string) => [...adminQueryKeys.stays(scope), stayId, 'consumption'] as const,
  experiences: (scope: string) => [...adminQueryKeys.root(scope), 'experiences'] as const,
  experienceCollections: (scope: string) => [...adminQueryKeys.experiences(scope), 'collections'] as const,
  experienceSlots: (scope: string, experienceId: string) => [...adminQueryKeys.experiences(scope), experienceId, 'slots'] as const,
  reservations: (scope: string) => [...adminQueryKeys.root(scope), 'reservations'] as const,
  reservationList: (scope: string, filters: { search: string; status: string }) => [...adminQueryKeys.reservations(scope), filters] as const,
  settings: (scope: string) => [...adminQueryKeys.root(scope), 'settings'] as const,
};
